# A CNAME inside the cluster pointing at a name outside it. No selector, no endpoints.
apiVersion: v1
kind: Service
metadata:
  name: web-externalname
spec:
  type: ExternalName
  externalName: example.com
